// Re-export game rules from the legacy utils source
// Ensures stable import surface: { BADGES, getRandomEvent, generateFeedback }
import { BADGES, getRandomEvent, generateFeedback } from '../utils/gameLogic';

export { BADGES, getRandomEvent, generateFeedback };


